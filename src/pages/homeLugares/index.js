import React from 'react'
import {
    View,
    Text,
    ImageBackground,
    TouchableOpacity,
    ScrollView,
  } from 'react-native';

export default function index({navigation}) {
    return (
       <Text> Hola mundo LUGARES HOME </Text>
    )
}
